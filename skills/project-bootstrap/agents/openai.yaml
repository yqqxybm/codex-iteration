interface:
  display_name: "Project Bootstrap"
  short_description: "Downstream new-project bootstrap stage."
  default_prompt: >
    Use $project-bootstrap when project-lifecycle assigns new-project bootstrap;
    scaffold a runnable vertical slice with verification and CodeGraph indexing.
